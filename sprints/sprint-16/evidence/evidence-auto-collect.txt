evidence.yml workflow_dispatch configured
name: Evidence Collection

on:
  workflow_dispatch:
    inputs:
      sprint:
        description: 'Sprint number'
        required: true
        type: string

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
RESULT: Evidence auto-collect PASS
