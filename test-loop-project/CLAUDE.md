# Test Loop Project

Simple project for testing the mission orchestration loop.

## Active Task
Add a `square_root(n)` function to `app.py` that returns the integer square root of a non-negative integer (use `int(n ** 0.5)`).
Add a test in `test_app.py` that verifies `square_root(9) == 3`, `square_root(16) == 4`, and `square_root(0) == 0`.

## Rules
- Keep responses short
- Run the tests and ensure they pass
- When task is done, say "gorev tamamlandi"
