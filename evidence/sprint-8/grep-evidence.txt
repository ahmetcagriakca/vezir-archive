=== Evidence: DataQuality 6-state (D-079) ===
7:- Fix 2: DataQuality enum (known_zero → fresh/partial)
26:    GPT Fix 2: known_zero removed. fresh/partial added.
29:    FRESH = "fresh"              # All sources present and below threshold
30:    PARTIAL = "partial"          # ≥1 source present but ≥1 missing
31:    STALE = "stale"              # ≥1 source above stale threshold
32:    DEGRADED = "degraded"        # ≥1 source parse error or circuit open
33:    UNKNOWN = "unknown"          # All sources missing — no data
34:    NOT_REACHED = "not_reached"  # Source not yet created (mission not started)
57:    status: DataQuality = DataQuality.UNKNOWN
63:    dataQuality: DataQuality = DataQuality.UNKNOWN
128:    dataQuality: DataQuality = DataQuality.UNKNOWN
141:    UNKNOWN = "unknown"
147:    status: CapabilityStatus = CapabilityStatus.UNKNOWN

=== Evidence: Wrapper Response classes ===
60:class ResponseMeta(BaseModel):
188:class MissionDetailResponse(BaseModel):
194:class MissionListResponse(BaseModel):
200:class StageListResponse(BaseModel):
206:class HealthResponse(BaseModel):
213:class ApprovalListResponse(BaseModel):
219:class TelemetryResponse(BaseModel):
225:class CapabilitiesResponse(BaseModel):

=== Evidence: CapabilityStatus tri-state ===
3:GPT Fix 4: Tri-state CapabilityStatus (available/unavailable/unknown).
10:from api.schemas import CapabilityEntry, CapabilityStatus
39:    def get_status(self, name: str) -> CapabilityStatus:
46:            return CapabilityStatus.UNKNOWN
49:            return CapabilityStatus.UNAVAILABLE
51:            return CapabilityStatus.AVAILABLE
52:        return CapabilityStatus.UNAVAILABLE
55:        """Convenience: get_status() == AVAILABLE."""
56:        return self.get_status(name) == CapabilityStatus.AVAILABLE
65:                status=(CapabilityStatus.AVAILABLE if available
66:                        else CapabilityStatus.UNAVAILABLE),

=== Evidence: Heartbeat background task ===
61:HEARTBEAT_INTERVAL_S = 30
66:    """Write/update services.json with heartbeat — GPT Fix 6.
91:        "heartbeatIntervalS": HEARTBEAT_INTERVAL_S,
129:    # Step 5: Start heartbeat background task (GPT Fix 6)
131:    heartbeat_task = asyncio.create_task(_heartbeat_loop())
136:    heartbeat_task.cancel()
138:        await heartbeat_task
145:async def _heartbeat_loop():
146:    """GPT Fix 6: Periodic heartbeat update to services.json."""
149:        await asyncio.sleep(HEARTBEAT_INTERVAL_S)

=== Evidence: TelemetryEntry missionId + sourceFile ===
10:- Fix 8: TelemetryEntry with missionId, sourceFile
109:    missionId: str
125:    missionId: str
167:    missionId: Optional[str] = None
175:# ── Telemetry (GPT Fix 8: missionId + sourceFile) ───────────────
181:    missionId: Optional[str] = None
182:    sourceFile: str = ""
